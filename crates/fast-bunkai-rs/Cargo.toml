[package]
name = "fast-bunkai-rs"
version = "0.1.0"
edition = "2021"
description = "Core sentence segmentation logic for fast-bunkai"
license = "Apache-2.0"

[lib]
name = "fast_bunkai_rs"

[dependencies]
